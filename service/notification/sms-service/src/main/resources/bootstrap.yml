# Spring Properties
spring:
  application:
     name: short_msg_service
  cloud:
    config:
      failFast: true
      label: master
      profile: dev
      discovery:
        enabled: true
        serviceId: short_msg_service

#Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://192.168.4.240:9001/eureka/
      #defaultZone: http://10.45.178.235:9001/eureka/
  instance:
    #hostname: 10.45.178.235
    hostname: 192.168.4.240
    
# HTTP Server 
server:
  port: 20000   # HTTP (Tomcat) port

  
  
